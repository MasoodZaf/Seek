# ⚠️ CRITICAL SECURITY WARNING ⚠️
# NEVER use these example values in production!
# Generate strong secrets with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# Each secret MUST be unique and at least 64 characters long

# Server Configuration
NODE_ENV=development
PORT=5001
API_VERSION=v1

# Database
MONGODB_URI=mongodb://localhost:27017/seek
MONGODB_TEST_URI=mongodb://localhost:27017/seek_test

# JWT Configuration
# PRODUCTION: Generate with crypto.randomBytes(64).toString('hex')
JWT_SECRET=REPLACE_WITH_RANDOM_64_CHAR_STRING_IN_PRODUCTION_DO_NOT_USE_THIS
JWT_EXPIRE=7d
# PRODUCTION: Must be different from JWT_SECRET
JWT_REFRESH_SECRET=REPLACE_WITH_DIFFERENT_64_CHAR_STRING_IN_PRODUCTION_DO_NOT_USE_THIS
JWT_REFRESH_EXPIRE=30d
COOKIE_EXPIRE=7

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Code Execution
CODE_EXECUTION_TIMEOUT=5000
MAX_CODE_LENGTH=10000

# Logging
LOG_LEVEL=info
LOG_FILE=logs/seek.log

# File Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=.js,.py,.java,.ts,.html,.css

# Email (for future use)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@seek.com

# AI Tutoring
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Error Tracking (Optional but recommended for production)
# Sign up at: https://sentry.io
SENTRY_DSN=

# Frontend URL (for CORS and Socket.io)
FRONTEND_URL=http://localhost:3000