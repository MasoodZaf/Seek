// 25 Python Tutorials with full 3-phase content
module.exports = function (T, S) {
  return [
    T({ title: 'Python Variables and Data Types', slug: 'py-variables-types', description: 'Learn Python variables, naming conventions, and built-in data types.', language: 'python', difficulty: 'beginner', duration: 30, tags: ['python', 'variables', 'types', 'fundamentals'], objectives: ['Declare and use variables', 'Understand Python data types', 'Use type() and type conversion'], featured: true, steps: [
      S(1, { title: 'Variables and Types', content: 'Python variables are dynamically typed — no type declaration needed.', lang: 'python', code: 'name = "Alice"\nage = 30\nheight = 5.6\nis_student = True\nprint(type(name), type(age), type(height), type(is_student))', concept: 'Python infers types from values. Key types: str, int, float, bool, None. Use type() to check.', keyPoints: ['Dynamic typing — no type declarations', 'str, int, float, bool, NoneType', 'type() returns the type', 'Variables are labels, not boxes'], realWorld: 'A user registration form stores name (str), age (int), email (str), is_verified (bool).', mistakes: ['Using reserved keywords as variable names', 'Confusing = (assignment) with == (comparison)', 'Forgetting Python is case-sensitive'], pInstructions: ['Create variables of each type', 'Print their types', 'Try type conversion'], starter: '# Create variables\nname = \nage = \npi = \nis_active = \n\n# Print types\nprint(type(name))\nprint(type(age))\n\n# Convert age to string\nage_str = \nprint(age_str, type(age_str))', solution: 'name = "Bob"\nage = 25\npi = 3.14159\nis_active = True\nprint(type(name))\nprint(type(age))\nage_str = str(age)\nprint(age_str, type(age_str))', hints: ['Strings use quotes', 'str() converts to string'], challenge: 'Write a function that takes any value and returns a human-readable type description.', reqs: ['Handle str, int, float, bool, None, list, dict', 'Return descriptive strings like "text" for str'], tests: [['describe(42)', '"integer number"', 5], ['describe("hi")', '"text"', 5]] }),
    ] }),

    T({ title: 'Python Strings and Formatting', slug: 'py-strings-formatting', description: 'Master string operations, f-strings, and text processing.', language: 'python', difficulty: 'beginner', duration: 35, tags: ['python', 'strings', 'formatting', 'text'], objectives: ['Use string methods', 'Format with f-strings', 'Slice and manipulate strings'], steps: [
      S(1, { title: 'String Methods and F-Strings', content: 'Strings are immutable sequences of characters with many useful methods.', lang: 'python', code: 'name = "alice"\nprint(name.upper())       # ALICE\nprint(name.capitalize())  # Alice\nprint(name.replace("a", "@"))  # @lice\n\n# F-strings\nage = 30\nprint(f"Name: {name.title()}, Age: {age}")', concept: 'Strings support methods like upper(), lower(), split(), join(), strip(). F-strings (f"...") allow embedded expressions.', keyPoints: ['Strings are immutable', 'Methods return new strings', 'F-strings for formatting', 'Slicing with [start:end:step]'], realWorld: 'Cleaning user input: strip whitespace, normalize case, validate format before storing in database.', mistakes: ['Forgetting strings are immutable', 'Off-by-one in slicing', 'Not using raw strings for regex/paths'], pInstructions: ['Use 5 string methods on a sample string', 'Format output using f-strings', 'Slice a string to extract parts'], starter: 'text = "  Hello, World!  "\n\n# Clean the text\ncleaned = \n\n# Extract "World"\nword = \n\n# Format a message\nname = "Python"\nversion = 3.12\nmessage = \n\nprint(cleaned)\nprint(word)\nprint(message)', solution: 'text = "  Hello, World!  "\ncleaned = text.strip()\nword = cleaned[7:12]\nname = "Python"\nversion = 3.12\nmessage = f"{name} version {version}"\nprint(cleaned)\nprint(word)\nprint(message)', hints: ['strip() removes whitespace', 'Slicing: text[start:end]'], challenge: 'Build a Caesar cipher function that shifts letters by a given amount.', reqs: ['Handle uppercase and lowercase', 'Wrap around z to a', 'Preserve non-letter characters'], tests: [['caesar("abc", 1)', '"bcd"', 5], ['caesar("xyz", 3)', '"abc"', 5]] }),
    ] }),

    T({ title: 'Python Lists and Tuples', slug: 'py-lists-tuples', description: 'Work with Python lists, tuples, and sequence operations.', language: 'python', difficulty: 'beginner', duration: 40, tags: ['python', 'lists', 'tuples', 'sequences'], objectives: ['Create and modify lists', 'Understand tuples vs lists', 'Use list comprehensions'], steps: [
      S(1, { title: 'List Operations', content: 'Lists are mutable ordered collections that support many operations.', lang: 'python', code: 'fruits = ["apple", "banana", "cherry"]\nfruits.append("date")\nfruits.insert(1, "blueberry")\nprint(fruits)\n\n# List comprehension\nsquares = [x**2 for x in range(1, 6)]\nprint(squares)  # [1, 4, 9, 16, 25]', concept: 'Lists are mutable sequences. List comprehensions provide concise syntax for creating lists from other iterables.', keyPoints: ['append(), insert(), remove(), pop()', 'Slicing creates copies', 'List comprehensions for transformation', 'sort() mutates, sorted() returns new'], realWorld: 'A playlist app uses a list for songs — add, remove, reorder, shuffle, filter by genre.', mistakes: ['Modifying a list while iterating', 'Confusing sort() and sorted()', 'Shallow copy of nested lists'], pInstructions: ['Create a list and use 5 operations', 'Write a list comprehension', 'Sort and filter the list'], starter: 'numbers = [5, 2, 8, 1, 9, 3, 7]\n\n# Sort a copy\nsorted_nums = \n\n# Filter even numbers using comprehension\nevens = \n\n# Double each number using comprehension\ndoubled = \n\nprint(sorted_nums)\nprint(evens)\nprint(doubled)', solution: 'numbers = [5, 2, 8, 1, 9, 3, 7]\nsorted_nums = sorted(numbers)\nevens = [n for n in numbers if n % 2 == 0]\ndoubled = [n * 2 for n in numbers]\nprint(sorted_nums)\nprint(evens)\nprint(doubled)', hints: ['sorted() returns new list', 'Comprehension: [expr for var in iterable if cond]'], challenge: 'Implement a flatten function that turns nested lists into a single flat list.', reqs: ['Handle any depth of nesting', 'Use recursion', 'Handle mixed types'], tests: [['flatten([1,[2,[3]]])', '[1,2,3]', 5], ['flatten([[1,2],[3]])', '[1,2,3]', 5]] }),
    ] }),

    T({ title: 'Python Dictionaries and Sets', slug: 'py-dicts-sets', description: 'Master dictionaries for key-value storage and sets for unique collections.', language: 'python', difficulty: 'beginner', duration: 35, tags: ['python', 'dictionaries', 'sets', 'data-structures'], objectives: ['Create and modify dictionaries', 'Use dict comprehensions', 'Perform set operations'], steps: [
      S(1, { title: 'Dictionary Operations', content: 'Dictionaries store key-value pairs with O(1) average lookup time.', lang: 'python', code: 'user = {"name": "Alice", "age": 30, "city": "NYC"}\nprint(user["name"])       # Alice\nprint(user.get("email", "N/A"))  # N/A\n\nuser["email"] = "alice@example.com"\nfor key, value in user.items():\n    print(f"{key}: {value}")', concept: 'Dictionaries use hash tables for fast lookups. Keys must be immutable (strings, numbers, tuples). Values can be anything.', keyPoints: ['O(1) average lookup', 'Keys must be hashable', '.get() with default prevents KeyError', 'Dict comprehensions for transformation'], realWorld: 'API responses are often JSON/dicts. User profiles, configs, and caches all use dictionaries.', mistakes: ['Accessing missing key without get()', 'Using mutable objects as keys', 'Not checking key existence before access'], pInstructions: ['Create a dictionary with 5 key-value pairs', 'Use get() with defaults', 'Write a dict comprehension'], starter: 'scores = {"Alice": 92, "Bob": 78, "Charlie": 95, "Diana": 88}\n\n# Get a score with default\nbobs_score = \n\n# Dict comprehension: only passing scores (>= 80)\npassing = \n\n# Add a new entry\n\nprint(bobs_score)\nprint(passing)', solution: 'scores = {"Alice": 92, "Bob": 78, "Charlie": 95, "Diana": 88}\nbobs_score = scores.get("Bob", 0)\npassing = {k: v for k, v in scores.items() if v >= 80}\nscores["Eve"] = 91\nprint(bobs_score)\nprint(passing)', hints: ['.get(key, default) avoids KeyError', 'Dict comprehension: {k: v for k, v in ...}'], challenge: 'Build a word frequency counter that processes text and returns sorted word counts.', reqs: ['Ignore case', 'Remove punctuation', 'Sort by frequency descending'], tests: [['count("the cat and the dog")', '{"the": 2, ...}', 5]] }),
    ] }),

    T({ title: 'Python Functions and Decorators', slug: 'py-functions-decorators', description: 'Write functions with default args, *args, **kwargs, and build decorators.', language: 'python', difficulty: 'intermediate', duration: 45, tags: ['python', 'functions', 'decorators', 'functional'], objectives: ['Use *args and **kwargs', 'Write lambda functions', 'Create and apply decorators'], featured: true, steps: [
      S(1, { title: 'Advanced Function Syntax', content: 'Python functions support flexible argument passing and first-class function features.', lang: 'python', code: 'def greet(name, greeting="Hello"):\n    return f"{greeting}, {name}!"\n\ndef summarize(*args, **kwargs):\n    print(f"Args: {args}")\n    print(f"Kwargs: {kwargs}")\n\nprint(greet("Alice"))\nprint(greet("Bob", greeting="Hi"))\nsummarize(1, 2, 3, x=10, y=20)', concept: '*args collects positional arguments as a tuple. **kwargs collects keyword arguments as a dict. This enables flexible function interfaces.', keyPoints: ['Default parameters for optional args', '*args for variable positional args', '**kwargs for variable keyword args', 'Functions are first-class objects'], realWorld: 'Django views use **kwargs for URL parameters. Logging functions use *args for flexible message formatting.', mistakes: ['Mutable default arguments (use None instead)', 'Wrong argument order: positional, *args, keyword, **kwargs', 'Modifying *args (it is a tuple)'], pInstructions: ['Write a function with default args', 'Write a function using *args', 'Write a function using **kwargs'], starter: 'def make_profile(name, **details):\n    # Build a profile dict from name and details\n    pass\n\ndef calculate(*numbers, operation="sum"):\n    # Perform operation on numbers\n    pass\n\nprint(make_profile("Alice", age=30, city="NYC"))\nprint(calculate(1, 2, 3, 4, 5))\nprint(calculate(1, 2, 3, 4, 5, operation="product"))', solution: 'def make_profile(name, **details):\n    return {"name": name, **details}\n\ndef calculate(*numbers, operation="sum"):\n    if operation == "sum":\n        return sum(numbers)\n    elif operation == "product":\n        result = 1\n        for n in numbers:\n            result *= n\n        return result\n    return 0\n\nprint(make_profile("Alice", age=30, city="NYC"))\nprint(calculate(1, 2, 3, 4, 5))\nprint(calculate(1, 2, 3, 4, 5, operation="product"))', hints: ['**details spreads into the dict', 'sum() works on tuples'], challenge: 'Write a decorator that caches function results (memoization).', reqs: ['Cache based on arguments', 'Return cached result for repeated calls', 'Add a .cache_clear() method'], tests: [['@cache applied', 'returns cached', 5], ['cache_clear works', 'true', 5]] }),
      S(2, { title: 'Decorators', content: 'Decorators wrap functions to add behavior without modifying them.', lang: 'python', code: 'import time\n\ndef timer(func):\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        elapsed = time.time() - start\n        print(f"{func.__name__} took {elapsed:.4f}s")\n        return result\n    return wrapper\n\n@timer\ndef slow_function():\n    total = sum(range(1000000))\n    return total\n\nresult = slow_function()', concept: 'A decorator is a function that takes a function and returns a modified version. The @decorator syntax is syntactic sugar for func = decorator(func).', keyPoints: ['Decorators wrap functions', '@syntax is sugar for reassignment', 'Use *args/**kwargs for flexibility', 'functools.wraps preserves metadata'], realWorld: 'Flask uses @app.route for URL routing. Django uses @login_required for auth checks.', mistakes: ['Not using functools.wraps', 'Not passing through args/kwargs', 'Decorator modifying function behavior unexpectedly'], pInstructions: ['Write a logging decorator', 'Write a retry decorator', 'Stack multiple decorators'], starter: 'def log_calls(func):\n    def wrapper(*args, **kwargs):\n        # Log the function call\n        # Call the original function\n        # Log the result\n        pass\n    return wrapper\n\n@log_calls\ndef add(a, b):\n    return a + b\n\nresult = add(3, 4)\nprint(result)', solution: 'def log_calls(func):\n    def wrapper(*args, **kwargs):\n        print(f"Calling {func.__name__} with {args}, {kwargs}")\n        result = func(*args, **kwargs)\n        print(f"{func.__name__} returned {result}")\n        return result\n    return wrapper\n\n@log_calls\ndef add(a, b):\n    return a + b\n\nresult = add(3, 4)\nprint(result)', hints: ['Call func(*args, **kwargs) inside wrapper', 'Return the result from wrapper'], challenge: 'Create a decorator with parameters: @repeat(n) that calls the function n times.', reqs: ['Accept number of repetitions', 'Collect all results in a list', 'Return list of results'], tests: [['@repeat(3) applied', 'calls 3 times', 5]] }),
    ] }),

    T({ title: 'Python Control Flow', slug: 'py-control-flow', description: 'Master if/elif/else, loops, and comprehensions in Python.', language: 'python', difficulty: 'beginner', duration: 30, tags: ['python', 'control-flow', 'loops', 'conditionals'], objectives: ['Write conditional statements', 'Use for and while loops', 'Apply break, continue, and else clauses'], steps: [
      S(1, { title: 'Conditionals and Loops', content: 'Python uses indentation for code blocks in if statements and loops.', lang: 'python', code: 'score = 85\nif score >= 90:\n    grade = "A"\nelif score >= 80:\n    grade = "B"\nelse:\n    grade = "C"\nprint(f"Grade: {grade}")\n\nfor i in range(5):\n    if i == 3:\n        continue\n    print(i, end=" ")', concept: 'If/elif/else for branching. For loops iterate over sequences. While loops repeat until a condition is false. break exits, continue skips.', keyPoints: ['Indentation defines blocks', 'elif instead of else if', 'range() for numeric loops', 'for/else runs if no break'], realWorld: 'A grading system uses if/elif to assign letter grades. A search uses for/else to check if item was found.', mistakes: ['Incorrect indentation', 'Infinite while loops', 'Forgetting colon after if/for/while'], pInstructions: ['Write an if/elif/else chain', 'Use a for loop with range', 'Use while with a break condition'], starter: '# FizzBuzz: print Fizz for 3, Buzz for 5, FizzBuzz for both\nfor i in range(1, 21):\n    # your code\n    pass', solution: 'for i in range(1, 21):\n    if i % 15 == 0:\n        print("FizzBuzz")\n    elif i % 3 == 0:\n        print("Fizz")\n    elif i % 5 == 0:\n        print("Buzz")\n    else:\n        print(i)', hints: ['Check divisible by 15 first', 'Use modulo (%) operator'], challenge: 'Implement a number guessing game that gives hints (higher/lower) and counts attempts.', reqs: ['Random target number', 'Loop until guessed', 'Count and display attempts'], tests: [['correct guess', 'Correct!', 5]] }),
    ] }),

    T({ title: 'Python File Handling', slug: 'py-file-handling', description: 'Read, write, and process files using Python.', language: 'python', difficulty: 'beginner', duration: 35, tags: ['python', 'files', 'io', 'text-processing'], objectives: ['Read and write text files', 'Use context managers', 'Process CSV data'], steps: [
      S(1, { title: 'Reading and Writing Files', content: 'Python uses the open() function with context managers for safe file handling.', lang: 'python', code: '# Writing\nwith open("example.txt", "w") as f:\n    f.write("Hello, World!\\n")\n    f.write("Python file handling")\n\n# Reading\nwith open("example.txt", "r") as f:\n    content = f.read()\n    print(content)\n\nimport os\nos.remove("example.txt")  # cleanup', concept: 'The with statement ensures files are properly closed. Modes: r (read), w (write), a (append), r+ (read/write).', keyPoints: ['with statement for auto-closing', 'read(), readline(), readlines()', 'write() and writelines()', 'Encoding with encoding="utf-8"'], realWorld: 'Log processing: read log files line by line, parse entries, filter errors, write summaries.', mistakes: ['Forgetting to close files (use with)', 'Not handling FileNotFoundError', 'Opening in wrong mode (w overwrites!)'], pInstructions: ['Write data to a file', 'Read it back', 'Process line by line'], starter: 'import os\n\n# Write some data\ndata = ["Alice,92\\n", "Bob,78\\n", "Charlie,95\\n"]\n\nwith open("scores.txt", "w") as f:\n    # Write all lines\n    pass\n\n# Read and process\nwith open("scores.txt", "r") as f:\n    for line in f:\n        # Parse name and score\n        pass\n\nos.remove("scores.txt")', solution: 'import os\ndata = ["Alice,92\\n", "Bob,78\\n", "Charlie,95\\n"]\nwith open("scores.txt", "w") as f:\n    f.writelines(data)\nwith open("scores.txt", "r") as f:\n    for line in f:\n        name, score = line.strip().split(",")\n        print(f"{name}: {score}")\nos.remove("scores.txt")', hints: ['writelines() writes a list of strings', 'strip() removes newlines, split() separates'], challenge: 'Build a simple CSV parser that reads a file and returns a list of dictionaries.', reqs: ['Use first row as headers', 'Handle quoted fields', 'Return list of dicts'], tests: [['parse CSV with 3 rows', 'list of 3 dicts', 5]] }),
    ] }),

    T({ title: 'Python Classes and OOP', slug: 'py-classes-oop', description: 'Object-oriented programming with classes, inheritance, and magic methods.', language: 'python', difficulty: 'intermediate', duration: 50, tags: ['python', 'classes', 'oop', 'inheritance'], objectives: ['Define classes with __init__', 'Use inheritance and polymorphism', 'Implement magic methods'], steps: [
      S(1, { title: 'Classes and Inheritance', content: 'Classes bundle data and behavior together using object-oriented principles.', lang: 'python', code: 'class Animal:\n    def __init__(self, name, sound):\n        self.name = name\n        self.sound = sound\n    def speak(self):\n        return f"{self.name} says {self.sound}"\n\nclass Dog(Animal):\n    def __init__(self, name):\n        super().__init__(name, "Woof")\n    def fetch(self):\n        return f"{self.name} fetches the ball"\n\ndog = Dog("Rex")\nprint(dog.speak())\nprint(dog.fetch())', concept: 'Classes create objects with shared structure. __init__ initializes instances. Inheritance lets subclasses extend parent behavior.', keyPoints: ['__init__ is the constructor', 'self refers to the instance', 'super() calls parent methods', 'isinstance() checks types'], realWorld: 'A payment system: Payment base class, CreditCardPayment and PayPalPayment subclasses with different processing logic.', mistakes: ['Forgetting self parameter', 'Not calling super().__init__()', 'Overusing inheritance (prefer composition)'], pInstructions: ['Create a base Vehicle class', 'Create Car and Motorcycle subclasses', 'Add specific methods to each'], starter: 'class Vehicle:\n    def __init__(self, make, model, year):\n        pass\n    def info(self):\n        pass\n\nclass Car(Vehicle):\n    def __init__(self, make, model, year, doors):\n        pass\n\nclass Motorcycle(Vehicle):\n    pass\n\ncar = Car("Toyota", "Camry", 2024, 4)\nprint(car.info())', solution: 'class Vehicle:\n    def __init__(self, make, model, year):\n        self.make = make\n        self.model = model\n        self.year = year\n    def info(self):\n        return f"{self.year} {self.make} {self.model}"\n\nclass Car(Vehicle):\n    def __init__(self, make, model, year, doors):\n        super().__init__(make, model, year)\n        self.doors = doors\n    def info(self):\n        return f"{super().info()} ({self.doors}-door)"\n\nclass Motorcycle(Vehicle):\n    def info(self):\n        return f"{super().info()} (motorcycle)"\n\ncar = Car("Toyota", "Camry", 2024, 4)\nprint(car.info())', hints: ['super().__init__() calls parent constructor', 'Override methods in subclasses'], challenge: 'Implement a BankAccount class with deposit, withdraw, and transfer methods, plus transaction history.', reqs: ['Prevent overdraft', 'Track all transactions', 'Transfer between accounts'], tests: [['deposit and withdraw', 'correct balance', 5]] }),
    ] }),

    T({ title: 'Python Error Handling', slug: 'py-error-handling', description: 'Handle exceptions with try/except, custom exceptions, and defensive programming.', language: 'python', difficulty: 'intermediate', duration: 35, tags: ['python', 'errors', 'exceptions', 'debugging'], objectives: ['Use try/except/finally', 'Create custom exceptions', 'Apply defensive programming'], steps: [
      S(1, { title: 'Exception Handling', content: 'Python uses try/except blocks to handle runtime errors gracefully.', lang: 'python', code: 'def safe_divide(a, b):\n    try:\n        result = a / b\n    except ZeroDivisionError:\n        return "Cannot divide by zero"\n    except TypeError:\n        return "Invalid types"\n    else:\n        return result\n    finally:\n        print("Division attempted")\n\nprint(safe_divide(10, 3))\nprint(safe_divide(10, 0))', concept: 'try/except catches specific exceptions. else runs if no exception. finally always runs. Custom exceptions extend Exception.', keyPoints: ['Catch specific exceptions', 'else clause for success path', 'finally for cleanup', 'raise to throw exceptions'], realWorld: 'API error handling: catch ConnectionError for network issues, TimeoutError for slow responses, ValueError for bad data.', mistakes: ['Catching bare Exception', 'Using exceptions for flow control', 'Silencing errors without logging'], pInstructions: ['Write a function with try/except', 'Handle multiple exception types', 'Create a custom exception'], starter: 'class InsufficientFundsError(Exception):\n    pass\n\ndef withdraw(balance, amount):\n    # Validate and withdraw\n    # Raise InsufficientFundsError if amount > balance\n    pass\n\ntry:\n    result = withdraw(100, 150)\nexcept InsufficientFundsError as e:\n    print(f"Error: {e}")', solution: 'class InsufficientFundsError(Exception):\n    def __init__(self, balance, amount):\n        self.balance = balance\n        self.amount = amount\n        super().__init__(f"Cannot withdraw ${amount}. Balance: ${balance}")\n\ndef withdraw(balance, amount):\n    if amount < 0:\n        raise ValueError("Amount must be positive")\n    if amount > balance:\n        raise InsufficientFundsError(balance, amount)\n    return balance - amount\n\ntry:\n    result = withdraw(100, 150)\nexcept InsufficientFundsError as e:\n    print(f"Error: {e}")', hints: ['Extend Exception class', 'super().__init__(message)'], challenge: 'Build a retry decorator that catches specific exceptions and retries up to N times.', reqs: ['Accept exception types to catch', 'Configurable max retries', 'Exponential backoff delay'], tests: [['succeeds on retry', 'returns result', 5]] }),
    ] }),

    T({ title: 'Python List Comprehensions', slug: 'py-list-comprehensions', description: 'Master list, dict, and set comprehensions for concise data transformations.', language: 'python', difficulty: 'beginner', duration: 30, tags: ['python', 'comprehensions', 'functional', 'pythonic'], objectives: ['Write list comprehensions', 'Use conditional comprehensions', 'Create dict and set comprehensions'], steps: [
      S(1, { title: 'Comprehension Syntax', content: 'Comprehensions provide a concise way to create sequences from other sequences.', lang: 'python', code: '# List comprehension\nsquares = [x**2 for x in range(10)]\nprint(squares)\n\n# With condition\nevens = [x for x in range(20) if x % 2 == 0]\nprint(evens)\n\n# Dict comprehension\nword_lengths = {w: len(w) for w in ["hello", "world", "python"]}\nprint(word_lengths)\n\n# Nested\nmatrix = [[i*j for j in range(1,4)] for i in range(1,4)]\nprint(matrix)', concept: 'Comprehensions are syntactic sugar for building lists, dicts, and sets from iterables with optional filtering and transformation.', keyPoints: ['[expr for var in iterable if cond]', '{key: val for ...} for dicts', '{expr for ...} for sets', 'Nested comprehensions for 2D data'], realWorld: 'Data processing pipelines: filter rows, transform columns, create lookup tables — all in one line.', mistakes: ['Overly complex comprehensions (use loops instead)', 'Nested comprehensions that are hard to read', 'Side effects in comprehensions'], pInstructions: ['Write a list comprehension with filtering', 'Write a dict comprehension', 'Write a nested comprehension'], starter: 'words = ["Hello", "WORLD", "Python", "IS", "awesome"]\n\n# Lowercase words longer than 3 chars\nlong_lower = \n\n# Word to length dict\nlengths = \n\n# Multiplication table 1-5\ntable = \n\nprint(long_lower)\nprint(lengths)\nprint(table)', solution: 'words = ["Hello", "WORLD", "Python", "IS", "awesome"]\nlong_lower = [w.lower() for w in words if len(w) > 3]\nlengths = {w: len(w) for w in words}\ntable = [[i*j for j in range(1,6)] for i in range(1,6)]\nprint(long_lower)\nprint(lengths)\nprint(table)', hints: ['Add if condition after the iterable', 'Dict comp uses {k: v for ...}'], challenge: 'Flatten a 2D matrix and find all prime numbers in it using comprehensions.', reqs: ['Flatten with nested comprehension', 'Filter primes', 'Return sorted unique primes'], tests: [['primes from [[2,4],[3,5,6]]', '[2,3,5]', 5]] }),
    ] }),

    T({ title: 'Python Modules and Packages', slug: 'py-modules-packages', description: 'Organize code with modules, imports, and the standard library.', language: 'python', difficulty: 'beginner', duration: 30, tags: ['python', 'modules', 'imports', 'stdlib'], objectives: ['Import modules and packages', 'Use the standard library', 'Create your own modules'], steps: [
      S(1, { title: 'Using Modules', content: 'Modules are Python files that contain reusable functions, classes, and variables.', lang: 'python', code: 'import math\nprint(math.sqrt(16))   # 4.0\nprint(math.pi)         # 3.14159...\n\nfrom datetime import datetime\nnow = datetime.now()\nprint(now.strftime("%Y-%m-%d"))\n\nimport random\nprint(random.randint(1, 100))', concept: 'import loads a module. from X import Y imports specific items. The standard library includes math, datetime, os, json, re, and many more.', keyPoints: ['import module — use module.func()', 'from module import func — use func()', 'as for aliases', 'Standard library has 200+ modules'], realWorld: 'Every Python project uses modules: os for file paths, json for API data, datetime for timestamps, re for validation.', mistakes: ['Circular imports', 'Importing everything with * (namespace pollution)', 'Shadowing module names with variable names'], pInstructions: ['Import and use math module', 'Import and use datetime', 'Use random for generating data'], starter: 'import math\nimport random\nfrom collections import Counter\n\n# Calculate hypotenuse of a 3-4-5 triangle\nhyp = \n\n# Generate 20 random numbers 1-10\nnumbers = \n\n# Count occurrences\ncounts = \n\nprint(f"Hypotenuse: {hyp}")\nprint(f"Most common: {counts.most_common(3)}")', solution: 'import math\nimport random\nfrom collections import Counter\nhyp = math.hypot(3, 4)\nnumbers = [random.randint(1, 10) for _ in range(20)]\ncounts = Counter(numbers)\nprint(f"Hypotenuse: {hyp}")\nprint(f"Most common: {counts.most_common(3)}")', hints: ['math.hypot(a, b) computes hypotenuse', 'Counter counts occurrences'], challenge: 'Build a command-line calculator using the math and operator modules.', reqs: ['Support +, -, *, /, ** operations', 'Handle invalid input gracefully', 'Support math functions like sqrt, sin, cos'], tests: [['calc("sqrt", 16)', '4.0', 5]] }),
    ] }),

    T({ title: 'Python Iterators and Generators', slug: 'py-iterators-generators', description: 'Create custom iterators and use generators for lazy evaluation.', language: 'python', difficulty: 'intermediate', duration: 40, tags: ['python', 'generators', 'iterators', 'lazy-evaluation'], objectives: ['Implement __iter__ and __next__', 'Write generator functions', 'Use generator expressions'], steps: [
      S(1, { title: 'Generators', content: 'Generators produce values on-demand using yield, enabling memory-efficient iteration.', lang: 'python', code: 'def fibonacci():\n    a, b = 0, 1\n    while True:\n        yield a\n        a, b = b, a + b\n\n# Take first 10\nfib = fibonacci()\nfor _ in range(10):\n    print(next(fib), end=" ")\nprint()\n\n# Generator expression\nsquares = (x**2 for x in range(1000000))\nprint(next(squares))  # 0\nprint(next(squares))  # 1', concept: 'Generators use yield to produce values lazily. Unlike lists, they don\'t store all values in memory. Generator expressions use () syntax.', keyPoints: ['yield pauses and produces a value', 'next() resumes the generator', 'Memory efficient for large data', 'Generator expressions: (expr for ...)'], realWorld: 'Processing large log files: a generator reads line by line instead of loading the entire file into memory.', mistakes: ['Exhausting a generator (can\'t reuse)', 'Using list() on infinite generator', 'Confusing generator expression with tuple'], pInstructions: ['Write a fibonacci generator', 'Create a generator expression', 'Chain generators together'], starter: 'def countdown(n):\n    # Yield n down to 1\n    pass\n\ndef take(n, gen):\n    # Take first n items from generator\n    pass\n\n# Use them\nfor num in countdown(5):\n    print(num, end=" ")\nprint()\n\nresult = take(5, (x**2 for x in range(100)))\nprint(result)', solution: 'def countdown(n):\n    while n > 0:\n        yield n\n        n -= 1\n\ndef take(n, gen):\n    result = []\n    for _ in range(n):\n        try:\n            result.append(next(gen))\n        except StopIteration:\n            break\n    return result\n\nfor num in countdown(5):\n    print(num, end=" ")\nprint()\n\nresult = take(5, (x**2 for x in range(100)))\nprint(result)', hints: ['yield returns a value and pauses', 'next() gets the next yielded value'], challenge: 'Build a pipeline of generators: read -> filter -> transform -> output.', reqs: ['Each stage is a generator', 'Lazy evaluation throughout', 'Support chaining arbitrary stages'], tests: [['pipeline processes data', 'correct output', 5]] }),
    ] }),

    T({ title: 'Python Lambda and Functional Programming', slug: 'py-lambda-functional', description: 'Use lambda, map, filter, reduce, and functools for functional programming.', language: 'python', difficulty: 'intermediate', duration: 35, tags: ['python', 'lambda', 'functional', 'map-filter-reduce'], objectives: ['Write lambda expressions', 'Use map, filter, reduce', 'Apply functools utilities'], steps: [
      S(1, { title: 'Lambda and Built-in Functions', content: 'Lambda creates anonymous functions. map/filter/reduce transform sequences functionally.', lang: 'python', code: 'from functools import reduce\n\nnumbers = [1, 2, 3, 4, 5]\n\ndoubled = list(map(lambda x: x * 2, numbers))\nprint(doubled)  # [2, 4, 6, 8, 10]\n\nevens = list(filter(lambda x: x % 2 == 0, numbers))\nprint(evens)  # [2, 4]\n\ntotal = reduce(lambda a, b: a + b, numbers)\nprint(total)  # 15', concept: 'Lambda creates small anonymous functions. map applies a function to each element. filter selects elements. reduce accumulates.', keyPoints: ['lambda x: expr for simple functions', 'map(func, iterable) transforms', 'filter(func, iterable) selects', 'reduce(func, iterable) accumulates'], realWorld: 'Data pipelines: map to transform records, filter to select valid ones, reduce to aggregate results.', mistakes: ['Overusing lambda (use def for complex logic)', 'Forgetting to wrap map/filter in list()', 'Not importing reduce from functools'], pInstructions: ['Use lambda with sorted() for custom sorting', 'Chain map and filter', 'Use reduce for aggregation'], starter: 'from functools import reduce\n\nstudents = [\n    {"name": "Alice", "grade": 92},\n    {"name": "Bob", "grade": 78},\n    {"name": "Charlie", "grade": 95},\n    {"name": "Diana", "grade": 88}\n]\n\n# Sort by grade descending\nsorted_students = \n\n# Get names of students with grade >= 85\nhonor_roll = \n\n# Calculate average grade\navg = \n\nprint([s["name"] for s in sorted_students])\nprint(honor_roll)\nprint(f"Average: {avg}")', solution: 'from functools import reduce\nstudents = [\n    {"name": "Alice", "grade": 92},\n    {"name": "Bob", "grade": 78},\n    {"name": "Charlie", "grade": 95},\n    {"name": "Diana", "grade": 88}\n]\nsorted_students = sorted(students, key=lambda s: s["grade"], reverse=True)\nhonor_roll = list(map(lambda s: s["name"], filter(lambda s: s["grade"] >= 85, students)))\navg = reduce(lambda a, b: a + b, [s["grade"] for s in students]) / len(students)\nprint([s["name"] for s in sorted_students])\nprint(honor_roll)\nprint(f"Average: {avg}")', hints: ['sorted() key parameter accepts a function', 'Chain filter inside map'], challenge: 'Implement your own versions of map, filter, and reduce using generators.', reqs: ['my_map returns a generator', 'my_filter returns a generator', 'my_reduce returns a single value'], tests: [['my_map(lambda x: x*2, [1,2,3])', '[2,4,6]', 5]] }),
    ] }),

    T({ title: 'Python Regular Expressions', slug: 'py-regex', description: 'Pattern matching and text processing with Python re module.', language: 'python', difficulty: 'intermediate', duration: 40, tags: ['python', 'regex', 'patterns', 'text-processing'], objectives: ['Write regex patterns', 'Use re.search, match, findall', 'Apply substitution and grouping'], steps: [
      S(1, { title: 'Pattern Matching with re', content: 'The re module provides powerful pattern matching for text processing.', lang: 'python', code: 'import re\n\ntext = "Contact: alice@example.com or bob@test.org"\nemails = re.findall(r"[\\w.-]+@[\\w.-]+\\.\\w+", text)\nprint(emails)\n\n# Groups\nmatch = re.search(r"(\\w+)@(\\w+\\.\\w+)", text)\nif match:\n    print(f"User: {match.group(1)}, Domain: {match.group(2)}")', concept: 'Regular expressions define patterns for matching text. findall returns all matches, search returns the first match object, sub replaces matches.', keyPoints: ['r"..." for raw strings', 'findall returns list of matches', 'search returns first Match object', 'Groups capture parts of the match'], realWorld: 'Parsing log files: extract timestamps, IP addresses, error codes using regex patterns.', mistakes: ['Not using raw strings', 'Greedy vs lazy matching', 'Not escaping special regex characters'], pInstructions: ['Extract phone numbers from text', 'Validate an email format', 'Use groups to parse structured data'], starter: 'import re\n\nlog = "2024-01-15 ERROR [auth] Login failed for user=admin ip=192.168.1.1"\n\n# Extract date\ndate = \n\n# Extract log level\nlevel = \n\n# Extract key=value pairs\npairs = \n\nprint(f"Date: {date}")\nprint(f"Level: {level}")\nprint(f"Pairs: {pairs}")', solution: 'import re\nlog = "2024-01-15 ERROR [auth] Login failed for user=admin ip=192.168.1.1"\ndate = re.search(r"\\d{4}-\\d{2}-\\d{2}", log).group()\nlevel = re.search(r"(INFO|ERROR|WARN|DEBUG)", log).group()\npairs = dict(re.findall(r"(\\w+)=(\\S+)", log))\nprint(f"Date: {date}")\nprint(f"Level: {level}")\nprint(f"Pairs: {pairs}")', hints: ['\\d{4} matches 4 digits', 'findall with groups returns tuples'], challenge: 'Build a simple markdown parser that converts **bold**, *italic*, and [links](url) to HTML.', reqs: ['Handle bold and italic', 'Handle links with text and URL', 'Preserve unmatched text'], tests: [['parse("**bold**")', '"<b>bold</b>"', 5]] }),
    ] }),

    T({ title: 'Python Context Managers', slug: 'py-context-managers', description: 'Use and create context managers with with statement and contextlib.', language: 'python', difficulty: 'intermediate', duration: 30, tags: ['python', 'context-managers', 'resources', 'with-statement'], objectives: ['Use built-in context managers', 'Create class-based context managers', 'Use contextlib decorators'], steps: [
      S(1, { title: 'Context Manager Protocol', content: 'Context managers ensure proper resource setup and cleanup using with statement.', lang: 'python', code: 'class Timer:\n    def __enter__(self):\n        import time\n        self.start = time.time()\n        return self\n    def __exit__(self, *args):\n        import time\n        self.elapsed = time.time() - self.start\n        print(f"Elapsed: {self.elapsed:.4f}s")\n\nwith Timer() as t:\n    total = sum(range(1000000))\n    print(f"Sum: {total}")', concept: 'Context managers implement __enter__ and __exit__. __enter__ sets up the resource, __exit__ cleans up, even if an exception occurs.', keyPoints: ['__enter__ returns the resource', '__exit__ handles cleanup', 'Exception info passed to __exit__', 'contextlib.contextmanager for simple cases'], realWorld: 'Database connections, file handles, network sockets, locks — all should be managed with context managers.', mistakes: ['Not implementing __exit__ properly', 'Swallowing exceptions in __exit__', 'Not using contextlib for simple cases'], pInstructions: ['Create a context manager class', 'Use contextlib.contextmanager', 'Handle exceptions in __exit__'], starter: 'from contextlib import contextmanager\n\n@contextmanager\ndef database_connection(db_name):\n    print(f"Connecting to {db_name}")\n    conn = {"db": db_name, "connected": True}\n    # yield the connection and handle cleanup\n    pass\n\nwith database_connection("mydb") as conn:\n    print(f"Using {conn}")', solution: 'from contextlib import contextmanager\n\n@contextmanager\ndef database_connection(db_name):\n    print(f"Connecting to {db_name}")\n    conn = {"db": db_name, "connected": True}\n    try:\n        yield conn\n    finally:\n        conn["connected"] = False\n        print(f"Disconnected from {db_name}")\n\nwith database_connection("mydb") as conn:\n    print(f"Using {conn}")', hints: ['yield inside try/finally', 'finally ensures cleanup runs'], challenge: 'Create a transaction context manager that commits on success and rollbacks on failure.', reqs: ['Auto-commit if no exception', 'Auto-rollback on exception', 'Support nested transactions'], tests: [['successful transaction', 'committed', 5]] }),
    ] }),

    T({ title: 'Python Data Classes', slug: 'py-dataclasses', description: 'Simplify class creation with dataclasses and named tuples.', language: 'python', difficulty: 'intermediate', duration: 30, tags: ['python', 'dataclasses', 'typing', 'oop'], objectives: ['Use @dataclass decorator', 'Add defaults and post_init', 'Compare with NamedTuple'], steps: [
      S(1, { title: 'Dataclass Basics', content: 'Dataclasses automatically generate __init__, __repr__, __eq__ and more.', lang: 'python', code: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n    \n    def distance(self, other):\n        return ((self.x - other.x)**2 + (self.y - other.y)**2)**0.5\n\np1 = Point(3, 4)\np2 = Point(0, 0)\nprint(p1)  # Point(x=3, y=4)\nprint(p1.distance(p2))  # 5.0\nprint(p1 == Point(3, 4))  # True', concept: 'The @dataclass decorator auto-generates boilerplate methods from class annotations. field() customizes individual fields.', keyPoints: ['Auto __init__, __repr__, __eq__', 'Type annotations define fields', 'field(default_factory=list) for mutables', 'frozen=True for immutable dataclasses'], realWorld: 'API response models: define the shape of data returned from endpoints as dataclasses.', mistakes: ['Mutable default values (use field(default_factory=))', 'Forgetting type annotations', 'Not using frozen for value objects'], pInstructions: ['Create a dataclass for a Product', 'Add computed properties', 'Make an immutable version'], starter: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Product:\n    name: str\n    price: float\n    quantity: int = 0\n    tags: list = field(default_factory=list)\n    \n    @property\n    def total_value(self):\n        # Return price * quantity\n        pass\n\np = Product("Widget", 9.99, 5, ["sale"])\nprint(p)\nprint(f"Total: ${p.total_value}")', solution: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Product:\n    name: str\n    price: float\n    quantity: int = 0\n    tags: list = field(default_factory=list)\n    \n    @property\n    def total_value(self):\n        return self.price * self.quantity\n\np = Product("Widget", 9.99, 5, ["sale"])\nprint(p)\nprint(f"Total: ${p.total_value}")', hints: ['field(default_factory=list) for mutable defaults', '@property for computed values'], challenge: 'Create a frozen dataclass for immutable configuration and add serialization to/from dict.', reqs: ['frozen=True for immutability', 'to_dict() method', 'from_dict() class method'], tests: [['Config is immutable', 'AttributeError on set', 5]] }),
    ] }),

    T({ title: 'Python Collections Module', slug: 'py-collections', description: 'Use Counter, defaultdict, OrderedDict, deque, and namedtuple.', language: 'python', difficulty: 'intermediate', duration: 35, tags: ['python', 'collections', 'data-structures', 'stdlib'], objectives: ['Use Counter for counting', 'Use defaultdict for auto-defaults', 'Use deque for efficient queues'], steps: [
      S(1, { title: 'Specialized Collections', content: 'The collections module provides specialized container datatypes.', lang: 'python', code: 'from collections import Counter, defaultdict, deque\n\n# Counter\nwords = "the quick brown fox jumps over the lazy dog the".split()\nword_counts = Counter(words)\nprint(word_counts.most_common(3))\n\n# defaultdict\ngrouped = defaultdict(list)\nfor w in words:\n    grouped[w[0]].append(w)\nprint(dict(grouped))\n\n# deque\ndq = deque([1, 2, 3])\ndq.appendleft(0)\ndq.append(4)\nprint(list(dq))', concept: 'Counter counts hashable objects. defaultdict provides default values for missing keys. deque is a double-ended queue with O(1) append/pop on both ends.', keyPoints: ['Counter.most_common(n) for top items', 'defaultdict(type) auto-creates defaults', 'deque for efficient queue operations', 'namedtuple for lightweight classes'], realWorld: 'Analytics: Counter for page views, defaultdict for grouping events, deque for recent activity log with fixed size.', mistakes: ['Using Counter on unhashable types', 'defaultdict with wrong factory type', 'Not specifying maxlen for bounded deque'], pInstructions: ['Use Counter to count characters', 'Use defaultdict to group data', 'Use deque as a fixed-size buffer'], starter: 'from collections import Counter, defaultdict, deque\n\ntext = "hello world"\n\n# Count characters\nchar_counts = \nprint(char_counts.most_common(3))\n\n# Group numbers by even/odd\nnumbers = [1, 2, 3, 4, 5, 6, 7, 8]\ngrouped = defaultdict(list)\n# group them\n\nprint(dict(grouped))\n\n# Recent items buffer (max 5)\nbuffer = deque(maxlen=5)\nfor i in range(10):\n    buffer.append(i)\nprint(list(buffer))', solution: 'from collections import Counter, defaultdict, deque\ntext = "hello world"\nchar_counts = Counter(text.replace(" ", ""))\nprint(char_counts.most_common(3))\nnumbers = [1, 2, 3, 4, 5, 6, 7, 8]\ngrouped = defaultdict(list)\nfor n in numbers:\n    grouped["even" if n % 2 == 0 else "odd"].append(n)\nprint(dict(grouped))\nbuffer = deque(maxlen=5)\nfor i in range(10):\n    buffer.append(i)\nprint(list(buffer))', hints: ['Counter works on any iterable', 'deque(maxlen=n) auto-removes old items'], challenge: 'Implement an LRU cache using OrderedDict.', reqs: ['Fixed capacity', 'Move accessed items to end', 'Evict oldest on overflow'], tests: [['cache hit moves to end', 'true', 5]] }),
    ] }),

    T({ title: 'Python Recursion', slug: 'py-recursion', description: 'Understand recursion, base cases, and recursive problem-solving patterns.', language: 'python', difficulty: 'intermediate', duration: 40, tags: ['python', 'recursion', 'algorithms', 'problem-solving'], objectives: ['Write recursive functions', 'Identify base cases', 'Convert between recursive and iterative'], steps: [
      S(1, { title: 'Recursive Thinking', content: 'Recursion solves problems by breaking them into smaller instances of the same problem.', lang: 'python', code: 'def factorial(n):\n    if n <= 1:  # base case\n        return 1\n    return n * factorial(n - 1)  # recursive case\n\nprint(factorial(5))  # 120\n\ndef fibonacci(n):\n    if n <= 1:\n        return n\n    return fibonacci(n-1) + fibonacci(n-2)\n\nprint([fibonacci(i) for i in range(10)])', concept: 'Every recursive function needs a base case (when to stop) and a recursive case (how to break the problem down). The call stack tracks each invocation.', keyPoints: ['Base case stops recursion', 'Recursive case reduces the problem', 'Call stack grows with each call', 'Memoization improves performance'], realWorld: 'File system traversal: list all files recursively by processing each directory\'s contents.', mistakes: ['Missing base case (infinite recursion)', 'Not reducing toward base case', 'Stack overflow on deep recursion'], pInstructions: ['Write a recursive sum function', 'Write a recursive power function', 'Add memoization to fibonacci'], starter: 'def power(base, exp):\n    # base^exp using recursion\n    pass\n\ndef sum_nested(lst):\n    # Sum all numbers in a nested list\n    pass\n\nprint(power(2, 10))  # 1024\nprint(sum_nested([1, [2, [3, 4], 5], 6]))  # 21', solution: 'def power(base, exp):\n    if exp == 0:\n        return 1\n    return base * power(base, exp - 1)\n\ndef sum_nested(lst):\n    total = 0\n    for item in lst:\n        if isinstance(item, list):\n            total += sum_nested(item)\n        else:\n            total += item\n    return total\n\nprint(power(2, 10))\nprint(sum_nested([1, [2, [3, 4], 5], 6]))', hints: ['Base case: exp == 0 returns 1', 'Check isinstance(item, list) for nesting'], challenge: 'Implement a recursive binary search and Tower of Hanoi solver.', reqs: ['Binary search with recursion', 'Tower of Hanoi with n disks', 'Print each move for Hanoi'], tests: [['binary_search([1,2,3], 2)', '1', 5], ['hanoi(3)', '7 moves', 5]] }),
    ] }),

    T({ title: 'Python Sorting and Searching', slug: 'py-sorting-searching', description: 'Implement sorting algorithms and searching techniques in Python.', language: 'python', difficulty: 'intermediate', duration: 45, tags: ['python', 'sorting', 'searching', 'algorithms'], category: 'Algorithms', objectives: ['Implement sorting algorithms', 'Use built-in sorting', 'Implement binary search'], steps: [
      S(1, { title: 'Sorting Algorithms', content: 'Different sorting algorithms have different performance characteristics.', lang: 'python', code: 'def merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(a, b):\n    result = []\n    i = j = 0\n    while i < len(a) and j < len(b):\n        if a[i] <= b[j]:\n            result.append(a[i]); i += 1\n        else:\n            result.append(b[j]); j += 1\n    return result + a[i:] + b[j:]\n\nprint(merge_sort([38, 27, 43, 3, 9, 82, 10]))', concept: 'Bubble sort O(n^2) is simple, merge sort O(n log n) is efficient, Python\'s sorted() uses Timsort O(n log n).', keyPoints: ['Bubble: O(n^2), simple, in-place', 'Merge: O(n log n), stable, extra space', 'Quick: O(n log n) avg, in-place', 'sorted() key parameter for custom sorting'], realWorld: 'E-commerce product sorting: by price, rating, relevance. Database indexes use sorted B-trees.', mistakes: ['Using O(n^2) sorts for large data', 'Not using the built-in sorted() when appropriate', 'Forgetting stability matters for equal elements'], pInstructions: ['Implement bubble sort', 'Implement merge sort', 'Compare with built-in sorted()'], starter: 'def bubble_sort(arr):\n    a = arr.copy()\n    # your code\n    return a\n\ndef quick_sort(arr):\n    # your code\n    pass\n\ndata = [64, 34, 25, 12, 22, 11, 90]\nprint("Bubble:", bubble_sort(data))\nprint("Quick:", quick_sort(data))\nprint("Built-in:", sorted(data))', solution: 'def bubble_sort(arr):\n    a = arr.copy()\n    n = len(a)\n    for i in range(n):\n        for j in range(n - i - 1):\n            if a[j] > a[j+1]:\n                a[j], a[j+1] = a[j+1], a[j]\n    return a\n\ndef quick_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    pivot = arr[len(arr)//2]\n    left = [x for x in arr if x < pivot]\n    mid = [x for x in arr if x == pivot]\n    right = [x for x in arr if x > pivot]\n    return quick_sort(left) + mid + quick_sort(right)\n\ndata = [64, 34, 25, 12, 22, 11, 90]\nprint("Bubble:", bubble_sort(data))\nprint("Quick:", quick_sort(data))\nprint("Built-in:", sorted(data))', hints: ['Bubble: swap adjacent if out of order', 'Quick: partition around pivot'], challenge: 'Implement counting sort and radix sort for integers.', reqs: ['Counting sort for small range', 'Radix sort for larger numbers', 'Compare performance'], tests: [['counting_sort([3,1,2])', '[1,2,3]', 5]] }),
    ] }),

    T({ title: 'Python Stack and Queue', slug: 'py-stack-queue', description: 'Implement stack and queue data structures in Python.', language: 'python', difficulty: 'beginner', duration: 35, tags: ['python', 'stack', 'queue', 'data-structures'], category: 'Data Structures', objectives: ['Implement stack with list', 'Implement queue with deque', 'Solve problems using both'], steps: [
      S(1, { title: 'Stack and Queue Implementations', content: 'Stack: LIFO (Last In, First Out). Queue: FIFO (First In, First Out).', lang: 'python', code: 'from collections import deque\n\n# Stack using list\nstack = []\nstack.append(1)\nstack.append(2)\nstack.append(3)\nprint(stack.pop())  # 3 (LIFO)\n\n# Queue using deque\nqueue = deque()\nqueue.append(1)\nqueue.append(2)\nqueue.append(3)\nprint(queue.popleft())  # 1 (FIFO)', concept: 'Python lists work as stacks (append/pop). For queues, use deque (append/popleft) for O(1) operations on both ends.', keyPoints: ['Stack: list.append() and list.pop()', 'Queue: deque.append() and deque.popleft()', 'list.pop(0) is O(n) - use deque instead', 'Priority queue: heapq module'], realWorld: 'Undo system uses a stack. Print queue and task scheduling use queues. Priority queues for job scheduling.', mistakes: ['Using list for queue (list.pop(0) is O(n))', 'Not checking empty before pop', 'Using deque when simple list works for stack'], pInstructions: ['Implement a Stack class', 'Implement a Queue class', 'Use stack to check balanced brackets'], starter: 'class Stack:\n    def __init__(self):\n        self.items = []\n    def push(self, item): pass\n    def pop(self): pass\n    def peek(self): pass\n    def is_empty(self): pass\n\ndef is_balanced(text):\n    """Check if brackets are balanced"""\n    stack = Stack()\n    # your code\n    pass\n\nprint(is_balanced("({[]})"))  # True\nprint(is_balanced("({[}])"))  # False', solution: 'class Stack:\n    def __init__(self):\n        self.items = []\n    def push(self, item):\n        self.items.append(item)\n    def pop(self):\n        return self.items.pop() if self.items else None\n    def peek(self):\n        return self.items[-1] if self.items else None\n    def is_empty(self):\n        return len(self.items) == 0\n\ndef is_balanced(text):\n    stack = Stack()\n    pairs = {")": "(", "]": "[", "}": "{"}\n    for ch in text:\n        if ch in "([{":\n            stack.push(ch)\n        elif ch in ")]}":\n            if stack.is_empty() or stack.pop() != pairs[ch]:\n                return False\n    return stack.is_empty()\n\nprint(is_balanced("({[]})"))  # True\nprint(is_balanced("({[}])"))  # False', hints: ['Push opening brackets, pop for closing', 'Check if popped bracket matches'], challenge: 'Implement a MinStack that supports getMin() in O(1) time.', reqs: ['push, pop, peek, getMin all O(1)', 'Track minimum at each level', 'Handle empty stack'], tests: [['push 3,1,2; getMin()', '1', 5]] }),
    ] }),

    T({ title: 'Python Binary Trees', slug: 'py-binary-trees', description: 'Implement binary trees with traversal, search, and balancing.', language: 'python', difficulty: 'intermediate', duration: 45, tags: ['python', 'trees', 'binary-tree', 'data-structures'], category: 'Data Structures', objectives: ['Build a binary search tree', 'Implement tree traversals', 'Search and insert nodes'], steps: [
      S(1, { title: 'BST Implementation', content: 'A binary search tree maintains sorted order with left < parent < right.', lang: 'python', code: 'class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n    def insert(self, val):\n        if not self.root:\n            self.root = Node(val)\n        else:\n            self._insert(self.root, val)\n    def _insert(self, node, val):\n        if val < node.val:\n            if not node.left: node.left = Node(val)\n            else: self._insert(node.left, val)\n        else:\n            if not node.right: node.right = Node(val)\n            else: self._insert(node.right, val)\n    def inorder(self, node=None, first=True):\n        if first: node = self.root\n        if not node: return []\n        return self.inorder(node.left, False) + [node.val] + self.inorder(node.right, False)\n\ntree = BST()\nfor v in [5, 3, 7, 1, 4, 6, 8]:\n    tree.insert(v)\nprint(tree.inorder())  # [1,3,4,5,6,7,8]', concept: 'BST maintains left < parent < right. Inorder traversal yields sorted order. Search, insert, delete are O(log n) average.', keyPoints: ['Left subtree < parent < right subtree', 'Inorder traversal gives sorted order', 'O(log n) average for balanced trees', 'O(n) worst case if degenerate'], realWorld: 'Databases use B-trees (generalized BSTs) for indexes. File systems use tree structures for directories.', mistakes: ['Forgetting to handle empty tree', 'Not considering degenerate (linked list) case', 'Confusing preorder/inorder/postorder'], pInstructions: ['Build a BST class with insert', 'Implement inorder traversal', 'Add search method'], starter: 'class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n    def insert(self, val): pass\n    def search(self, val): pass\n    def inorder(self): pass\n\ntree = BST()\nfor v in [5, 3, 7, 1, 4]:\n    tree.insert(v)\nprint(tree.inorder())\nprint(tree.search(4))  # True\nprint(tree.search(6))  # False', solution: 'class Node:\n    def __init__(self, val):\n        self.val = val\n        self.left = None\n        self.right = None\n\nclass BST:\n    def __init__(self):\n        self.root = None\n    def insert(self, val):\n        self.root = self._ins(self.root, val)\n    def _ins(self, node, val):\n        if not node: return Node(val)\n        if val < node.val: node.left = self._ins(node.left, val)\n        else: node.right = self._ins(node.right, val)\n        return node\n    def search(self, val):\n        return self._search(self.root, val)\n    def _search(self, node, val):\n        if not node: return False\n        if val == node.val: return True\n        if val < node.val: return self._search(node.left, val)\n        return self._search(node.right, val)\n    def inorder(self):\n        result = []\n        self._inorder(self.root, result)\n        return result\n    def _inorder(self, node, result):\n        if node:\n            self._inorder(node.left, result)\n            result.append(node.val)\n            self._inorder(node.right, result)\n\ntree = BST()\nfor v in [5, 3, 7, 1, 4]:\n    tree.insert(v)\nprint(tree.inorder())\nprint(tree.search(4))\nprint(tree.search(6))', hints: ['Insert recursively by comparing values', 'Search follows the same path as insert'], challenge: 'Add a delete method and implement level-order (BFS) traversal.', reqs: ['Delete handles all 3 cases (leaf, one child, two children)', 'BFS uses a queue', 'Return nodes level by level'], tests: [['delete leaf', 'removed', 5], ['BFS order', '[5,3,7,1,4]', 5]] }),
    ] }),

    T({ title: 'Python Graph Algorithms', slug: 'py-graph-algorithms', description: 'Implement graphs and traverse them with BFS and DFS.', language: 'python', difficulty: 'advanced', duration: 50, tags: ['python', 'graphs', 'bfs', 'dfs', 'algorithms'], category: 'Algorithms', objectives: ['Represent graphs', 'Implement BFS and DFS', 'Find shortest paths'], steps: [
      S(1, { title: 'Graph Traversal', content: 'Graphs consist of vertices connected by edges. BFS and DFS are fundamental traversals.', lang: 'python', code: 'from collections import deque\n\ndef bfs(graph, start):\n    visited = set()\n    queue = deque([start])\n    order = []\n    while queue:\n        node = queue.popleft()\n        if node not in visited:\n            visited.add(node)\n            order.append(node)\n            queue.extend(n for n in graph[node] if n not in visited)\n    return order\n\ngraph = {\n    "A": ["B", "C"],\n    "B": ["A", "D", "E"],\n    "C": ["A", "F"],\n    "D": ["B"], "E": ["B", "F"], "F": ["C", "E"]\n}\nprint(bfs(graph, "A"))', concept: 'BFS explores level by level using a queue. DFS explores as deep as possible using a stack or recursion. Both visit each node once.', keyPoints: ['BFS uses queue — level-order traversal', 'DFS uses stack/recursion — depth-first', 'BFS finds shortest path in unweighted graphs', 'Track visited to avoid cycles'], realWorld: 'Social network friend suggestions (BFS for nearby connections), web crawlers (DFS for following links deeply).', mistakes: ['Not tracking visited nodes (infinite loop)', 'Wrong data structure (queue vs stack)', 'Not handling disconnected components'], pInstructions: ['Implement BFS traversal', 'Implement DFS traversal', 'Find shortest path with BFS'], starter: 'from collections import deque\n\ndef dfs(graph, start, visited=None):\n    # Recursive DFS\n    pass\n\ndef shortest_path(graph, start, end):\n    # BFS-based shortest path\n    pass\n\ngraph = {"A": ["B","C"], "B": ["A","D"], "C": ["A","D"], "D": ["B","C"]}\nprint(dfs(graph, "A"))\nprint(shortest_path(graph, "A", "D"))', solution: 'from collections import deque\n\ndef dfs(graph, start, visited=None):\n    if visited is None:\n        visited = []\n    visited.append(start)\n    for neighbor in graph[start]:\n        if neighbor not in visited:\n            dfs(graph, neighbor, visited)\n    return visited\n\ndef shortest_path(graph, start, end):\n    queue = deque([(start, [start])])\n    visited = {start}\n    while queue:\n        node, path = queue.popleft()\n        if node == end:\n            return path\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append((neighbor, path + [neighbor]))\n    return None\n\ngraph = {"A": ["B","C"], "B": ["A","D"], "C": ["A","D"], "D": ["B","C"]}\nprint(dfs(graph, "A"))\nprint(shortest_path(graph, "A", "D"))', hints: ['DFS: recurse on unvisited neighbors', 'BFS shortest path: track path in queue'], challenge: 'Implement Dijkstra\'s algorithm for weighted shortest paths.', reqs: ['Handle weighted edges', 'Use a priority queue', 'Return distance and path'], tests: [['dijkstra on weighted graph', 'shortest distance', 5]] }),
    ] }),

    T({ title: 'Python Linked Lists', slug: 'py-linked-lists', description: 'Implement and manipulate singly and doubly linked lists.', language: 'python', difficulty: 'intermediate', duration: 40, tags: ['python', 'linked-list', 'data-structures', 'pointers'], category: 'Data Structures', objectives: ['Build a linked list from scratch', 'Insert, delete, and search', 'Reverse a linked list'], steps: [
      S(1, { title: 'Singly Linked List', content: 'A linked list stores elements in nodes connected by pointers.', lang: 'python', code: 'class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    def append(self, data):\n        node = Node(data)\n        if not self.head:\n            self.head = node\n            return\n        curr = self.head\n        while curr.next:\n            curr = curr.next\n        curr.next = node\n    def display(self):\n        vals = []\n        curr = self.head\n        while curr:\n            vals.append(str(curr.data))\n            curr = curr.next\n        print(" -> ".join(vals))\n\nll = LinkedList()\nfor v in [1, 2, 3, 4]:\n    ll.append(v)\nll.display()', concept: 'Each node stores data and a reference to the next node. The list only stores a reference to the head. Operations traverse from head.', keyPoints: ['O(1) insert at head', 'O(n) insert at tail (without tail pointer)', 'O(n) search', 'No random access like arrays'], realWorld: 'Music playlist: each song points to the next. Browser tabs as a linked structure.', mistakes: ['Losing reference to head', 'Not handling empty list', 'Off-by-one errors in traversal'], pInstructions: ['Create a LinkedList class', 'Add prepend and delete methods', 'Implement reverse'], starter: 'class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    def append(self, data): pass\n    def prepend(self, data): pass\n    def delete(self, data): pass\n    def reverse(self): pass\n    def display(self):\n        vals, curr = [], self.head\n        while curr: vals.append(str(curr.data)); curr = curr.next\n        print(" -> ".join(vals) or "empty")\n\nll = LinkedList()\nfor v in [1,2,3,4]: ll.append(v)\nll.display()\nll.reverse()\nll.display()', solution: 'class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n    def append(self, data):\n        n = Node(data)\n        if not self.head: self.head = n; return\n        c = self.head\n        while c.next: c = c.next\n        c.next = n\n    def prepend(self, data):\n        n = Node(data)\n        n.next = self.head\n        self.head = n\n    def delete(self, data):\n        if not self.head: return\n        if self.head.data == data: self.head = self.head.next; return\n        c = self.head\n        while c.next and c.next.data != data: c = c.next\n        if c.next: c.next = c.next.next\n    def reverse(self):\n        prev, curr = None, self.head\n        while curr:\n            nxt = curr.next\n            curr.next = prev\n            prev = curr\n            curr = nxt\n        self.head = prev\n    def display(self):\n        vals, curr = [], self.head\n        while curr: vals.append(str(curr.data)); curr = curr.next\n        print(" -> ".join(vals) or "empty")\n\nll = LinkedList()\nfor v in [1,2,3,4]: ll.append(v)\nll.display()\nll.reverse()\nll.display()', hints: ['Reverse: track prev, curr, next', 'Delete: find node before target'], challenge: 'Detect if two linked lists merge (intersect) and find the merge point.', reqs: ['O(n+m) time complexity', 'O(1) extra space', 'Handle no intersection'], tests: [['intersecting lists', 'merge point found', 5]] }),
    ] }),

    T({ title: 'Python Hash Tables', slug: 'py-hash-tables', description: 'Understand hash tables, implement a simple one, and solve hashing problems.', language: 'python', difficulty: 'intermediate', duration: 40, tags: ['python', 'hash-table', 'hashing', 'data-structures'], category: 'Data Structures', objectives: ['Understand hash function concepts', 'Implement a simple hash table', 'Solve problems using hashing'], steps: [
      S(1, { title: 'Hash Table Implementation', content: 'Hash tables provide O(1) average lookup by mapping keys to array indices.', lang: 'python', code: 'class HashTable:\n    def __init__(self, size=10):\n        self.size = size\n        self.table = [[] for _ in range(size)]\n    def _hash(self, key):\n        return hash(key) % self.size\n    def put(self, key, value):\n        idx = self._hash(key)\n        for i, (k, v) in enumerate(self.table[idx]):\n            if k == key:\n                self.table[idx][i] = (key, value)\n                return\n        self.table[idx].append((key, value))\n    def get(self, key):\n        idx = self._hash(key)\n        for k, v in self.table[idx]:\n            if k == key:\n                return v\n        raise KeyError(key)\n\nht = HashTable()\nht.put("name", "Alice")\nht.put("age", 30)\nprint(ht.get("name"))\nprint(ht.get("age"))', concept: 'Hash tables use a hash function to map keys to bucket indices. Collisions are handled by chaining (linked lists in each bucket).', keyPoints: ['Hash function maps key to index', 'Chaining handles collisions', 'O(1) average, O(n) worst case', 'Load factor affects performance'], realWorld: 'Python dict is a hash table. Caches, sets, and database indexes all use hashing.', mistakes: ['Poor hash function causing many collisions', 'Not handling key updates', 'Forgetting to resize when load factor is high'], pInstructions: ['Implement a HashTable class', 'Add put, get, delete methods', 'Handle collisions with chaining'], starter: 'class HashTable:\n    def __init__(self, size=10):\n        self.size = size\n        self.table = [[] for _ in range(size)]\n    def _hash(self, key): return hash(key) % self.size\n    def put(self, key, value): pass\n    def get(self, key): pass\n    def delete(self, key): pass\n    def keys(self): pass\n\nht = HashTable()\nht.put("a", 1)\nht.put("b", 2)\nprint(ht.get("a"))\nht.delete("a")\nprint(ht.keys())', solution: 'class HashTable:\n    def __init__(self, size=10):\n        self.size = size\n        self.table = [[] for _ in range(size)]\n    def _hash(self, key): return hash(key) % self.size\n    def put(self, key, value):\n        idx = self._hash(key)\n        for i, (k, v) in enumerate(self.table[idx]):\n            if k == key: self.table[idx][i] = (key, value); return\n        self.table[idx].append((key, value))\n    def get(self, key):\n        for k, v in self.table[self._hash(key)]:\n            if k == key: return v\n        raise KeyError(key)\n    def delete(self, key):\n        idx = self._hash(key)\n        self.table[idx] = [(k, v) for k, v in self.table[idx] if k != key]\n    def keys(self):\n        return [k for bucket in self.table for k, v in bucket]\n\nht = HashTable()\nht.put("a", 1)\nht.put("b", 2)\nprint(ht.get("a"))\nht.delete("a")\nprint(ht.keys())', hints: ['Use list of tuples for chaining', 'Filter out matching key for delete'], challenge: 'Find the first non-repeating character in a string using a hash table.', reqs: ['O(n) time complexity', 'Use hash table for counting', 'Return character or None'], tests: [['first_unique("aabbc")', '"c"', 5], ['first_unique("aabb")', 'None', 5]] }),
    ] }),

  ];
};
