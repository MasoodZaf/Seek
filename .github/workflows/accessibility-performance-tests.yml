name: Accessibility & Performance Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run tests daily at 2 AM UTC
    - cron: '0 2 * * *'

jobs:
  accessibility-performance-tests:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: |
          frontend/package-lock.json
          backend/package-lock.json

    - name: Install frontend dependencies
      run: |
        cd frontend
        npm ci

    - name: Install backend dependencies
      run: |
        cd backend
        npm ci

    - name: Start backend server
      run: |
        cd backend
        npm start &
        # Wait for server to be ready (up to 30 seconds)
        echo "Waiting for backend server to start..."
        for i in {1..30}; do
          if curl -f http://localhost:5001/api/v1/health > /dev/null 2>&1; then
            echo "✅ Backend server is ready!"
            break
          fi
          echo "Waiting for server... ($i/30)"
          sleep 1
        done
        # Final health check
        curl -f http://localhost:5001/api/v1/health || (echo "❌ Backend server failed to start" && exit 1)
      env:
        NODE_ENV: test
        PORT: 5001
        MONGODB_URI: ${{ secrets.MONGODB_URI || 'mongodb://localhost:27017/seek-test' }}
        JWT_SECRET: ${{ secrets.JWT_SECRET || 'test-secret-key' }}
        FRONTEND_URL: http://localhost:3000
    
    - name: Build frontend
      run: |
        cd frontend
        npm run build
      env:
        CI: true
        REACT_APP_API_URL: http://localhost:5001/api/v1
        GENERATE_SOURCEMAP: false

    - name: Start frontend (for E2E tests)
      run: |
        cd frontend
        npx serve -s build -l 3000 &
        # Wait for frontend to be ready
        echo "Waiting for frontend server to start..."
        for i in {1..20}; do
          if curl -f http://localhost:3000 > /dev/null 2>&1; then
            echo "✅ Frontend server is ready!"
            break
          fi
          echo "Waiting for frontend... ($i/20)"
          sleep 1
        done
      continue-on-error: false

    - name: Run accessibility tests
      run: |
        cd frontend
        if grep -q "test:accessibility" package.json; then
          echo "Running accessibility tests..."
          npm run test:accessibility
        else
          echo "⚠️  Skipping accessibility tests - script not found"
        fi
      continue-on-error: true
      env:
        CI: true

    - name: Run performance tests
      run: |
        cd frontend
        if grep -q "test:performance" package.json; then
          echo "Running performance tests..."
          npm run test:performance
        else
          echo "⚠️  Skipping performance tests - script not found"
        fi
      continue-on-error: true
      env:
        CI: true
    
    - name: Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports-${{ matrix.node-version }}
        path: |
          frontend/test-reports/
          frontend/.lighthouseci/
        retention-days: 30

    - name: Upload coverage reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: coverage-reports-${{ matrix.node-version }}
        path: frontend/coverage/
        retention-days: 30
    
    - name: Comment PR with test results
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const path = require('path');
          
          try {
            const reportPath = path.join('frontend/test-reports/accessibility-performance-report.json');
            if (fs.existsSync(reportPath)) {
              const report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
              
              const comment = `
              ## 🧪 Accessibility & Performance Test Results
              
              **Overall Status:** ${report.summary.overallStatus === 'passed' ? '✅ PASSED' : '❌ FAILED'}
              **Duration:** ${Math.round(report.summary.totalDuration / 1000)}s
              
              ### Test Summary
              | Category | Passed | Failed | Total |
              |----------|--------|--------|-------|
              | Accessibility | ${report.summary.testCounts.accessibility.passed} | ${report.summary.testCounts.accessibility.failed} | ${report.summary.testCounts.accessibility.total} |
              | Performance | ${report.summary.testCounts.performance.passed} | ${report.summary.testCounts.performance.failed} | ${report.summary.testCounts.performance.total} |
              | Keyboard Navigation | ${report.summary.testCounts.keyboardNavigation.passed} | ${report.summary.testCounts.keyboardNavigation.failed} | ${report.summary.testCounts.keyboardNavigation.total} |
              
              📊 [View detailed report in artifacts](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})
              `;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            }
          } catch (error) {
            console.log('Could not post test results comment:', error.message);
          }

  lighthouse-budget-check:
    runs-on: ubuntu-latest
    needs: accessibility-performance-tests
    if: always()

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download test artifacts
      uses: actions/download-artifact@v4
      continue-on-error: true
      with:
        name: test-reports-18.x
        path: test-reports/

    - name: Check test results
      run: |
        echo "✅ Test artifacts check completed"
        if [ -d "test-reports" ]; then
          echo "📊 Test reports found"
          ls -la test-reports/ || true
        else
          echo "⚠️ No test reports directory found (this is okay)"
        fi